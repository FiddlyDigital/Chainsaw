name: CI

on:
  push:
    branches: [main, master]
  pull_request:

# A second push to a branch makes the first run's answer irrelevant.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  check:
    name: Lint, typecheck and test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: '22'
          cache: npm

      - run: npm ci

      # Separate steps so a failure names itself in the checks list rather than
      # making someone read a log to find out what broke.
      - name: Lint
        run: npm run lint

      - name: Formatting
        run: npm run format:check

      - name: Types
        run: npm run typecheck

      - name: Unit tests
        run: npm run test:coverage

      - name: Coverage report
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: coverage
          path: coverage/
          retention-days: 7

  e2e:
    # The end-to-end suite runs against the built app, in a real browser with a
    # real AudioContext: the service worker, the offline shell, and whether any
    # sound fails to resolve are all part of what is being checked.
    #
    # Two bases, because everything the app builds — asset URLs, the manifest's
    # scope, the service worker's scope and precache — is base-relative, and
    # that has a way of working at exactly one of a domain root and a folder.
    # GitHub Pages serves the folder case, so it is not optional.
    name: End to end (${{ matrix.base }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        base: ['/', '/Chainsaw/']
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: '22'
          cache: npm

      - run: npm ci

      - name: Build
        run: npm run build

      # Only Chromium: the full set is most of a gigabyte of downloads for
      # tests that never open Firefox or WebKit.
      - name: Install Chromium
        run: npx playwright install --with-deps chromium

      - name: Run the suite
        run: npm run test:e2e
        env:
          BASE_PATH: ${{ matrix.base }}

      - name: Report
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          # Traces and screenshots from the failure, which is the only time
          # anyone wants them.
          name: playwright-report-${{ strategy.job-index }}
          path: |
            playwright-report/
            test-results/
          retention-days: 7
