/**
 * Where the source lives, and why the app says so on screen.
 *
 * Chainsaw is AGPL-3.0-or-later, which it must be: it links against Strudel,
 * which is AGPL. Section 13 of that licence says that if users interact with
 * the program remotely over a network — which is exactly what a deployed
 * Chainsaw is — they must be *prominently* offered its Corresponding Source.
 *
 * So the transport bar carries a permanent link to it. That is a licence
 * obligation, not decoration: it should not be removed to save space, and if
 * you fork and deploy this, point it at your fork.
 */
export const SOURCE_URL = 'https://github.com/FiddlyDigital/Chainsaw'

export const LICENSE = 'AGPL-3.0-or-later'
